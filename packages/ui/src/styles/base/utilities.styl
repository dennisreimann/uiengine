.uie
  $spaces = json(pathjoin('tokens', 'spaces.json'), { hash: true })
  for $name, $value in $spaces
    .uie-sot-{$name}
      margin-top s("var(--uie-space-%s)", unquote($name))
    .uie-sob-{$name}
      margin-bottom s("var(--uie-space-%s)", unquote($name))

extends _layout

block main
  section.page(id=h.dasherize(page.id))
    header.page__header
      h1.page__title= page.title
    article.page__content
      !=page.content

    h2 Sitemap
    nav.sitemap
      +nav(navigation['index'].childIds, 'sitemap')

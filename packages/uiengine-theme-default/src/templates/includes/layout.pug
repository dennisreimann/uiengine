include /main/main.pug
include /mainnav/mainnav.pug

doctype
html
  head
    title #{page.title} • #{config.name} (#{config.version})
    meta(http-equiv="Content-Type" content="text/html;charset=UTF-8")
    meta(name="viewport" content="width=device-width,initial-scale=1")
    link(href=h.assetPath("styles/uiengine.css") rel="stylesheet")
    link(href="https://fonts.googleapis.com/css?family=Merriweather+Sans:300i,400,800" rel="stylesheet")
  body
    +mainnav()
    +main()
      block main
    script(src=h.assetPath("scripts/uiengine.js") defer)

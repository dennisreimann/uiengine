.variation-preview
  &__iframe
    display block
    width 100%
    margin 0 auto
    border-top 1px solid $color-gray-20

    // $breakpoints = json('../../styles/lib/breakpoints.json', {})
    // for $breakpoint-name in $breakpoints
    //   $breakpoint-width = $breakpoints[$breakpoint-name]
    //   &[data-breakpoint={$breakpoint-name}]
    //     width $breakpoint-width

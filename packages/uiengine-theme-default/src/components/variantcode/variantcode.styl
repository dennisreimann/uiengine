.variantcode
  margin-top $space-large

  &__sections
    margin-right $space-normal * -1
    margin-left $space-normal * -1

  &__header
    display flex
    align-items center
    justify-content space-between
    border 1px solid $color-neutral-20
    padding $space-small $space-normal
    cursor pointer
    color $color-code-header

    &:hover
      color $color-code-header-hover

  &__section:not(.variantcode__section--expanded) + &__section &__header
    border-top 0

  &__expandicon
    transition-property transform
    transition-duration .15s
    icon-color($color-code-header)

    .variantcode__header:hover &
      icon-color($color-code-header-hover)

    .variantcode__section--expanded &
      transform rotate(-180deg)

  &__content
    display none

    .variantcode__section--expanded &
      display block

  &__raw
  &__context
  &__rendered
    overflow-x auto
    font-family $font-family-code
    font-size $font-size-small
    // defaults, will be overriden by hljs
    color $color-code-text
    background-color $color-code-bg
    // use important to override hljs styling which differs per theme
    padding $space-normal !important


include /icon/icon.pug

mixin permalink(link, text)
  a.permalink(href=link data-clipboard-text=link)&attributes(attributes)
    +icon('link-45').permalink__icon
    if text
      span.permalink__text= text
